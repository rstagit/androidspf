<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidSpf"
        tools:targetApi="34">

        <activity
            android:name=".ui.dashboard.LaunchActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.setup.ConfigActivity"
            android:exported="false"
            android:theme="@style/Theme.AndroidSpf.ActionBar"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".ui.dashboard.LaunchActivity" />

        <activity
            android:name=".ui.logview.LogActivity"
            android:exported="false"
            android:theme="@style/Theme.AndroidSpf.ActionBar"
            android:parentActivityName=".ui.dashboard.LaunchActivity" />

        <service
            android:name=".service.TunnelForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>

</manifest>
